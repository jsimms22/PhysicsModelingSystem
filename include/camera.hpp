#pragma once

// std library
#include <cmath>
// project headers
#include "math.hpp"

// constexpr vec3f GLOBAL_UP = { 0.0, 1.0, 0.0 };

// struct Camera
// {
//     vec3f position;
//     float yaw;
//     float pitch;
//     vec3f forwards;
//     vec3f right;
//     vec3f up;
// };

// Camera createCamera(vec3f position);

// void updateVectors(Camera camera);

// mfloat_t* createViewMatrix(mfloat_t* viewMatrix, Camera* camera);